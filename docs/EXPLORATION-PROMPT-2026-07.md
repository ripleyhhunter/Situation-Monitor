# Situation Monitor — Autonomous Feature & Source Build

*Written for Claude Fable 5, July 2026. Run in a Claude Code session on the `C:\Users\Ripley\Situation-Monitor` repo. Built for maximal autonomy: it decides and builds without waiting on me. A working tool, not project documentation — commit it, move it, or delete it as you like.*

---

I own Situation Monitor. It just came through a full assessment-and-fix cycle — `master` is stable, CI-gated, and every known bug is fixed (`docs/ASSESSMENT-2026-07.md`, `docs/ROADMAP-2026-07.md`). Your job now is to make it meaningfully better on your own: explore new features and upgrades to the existing data sources, decide what's worth building, and build as much of it as you can in the largest sensible sequence of actions — without stopping to check in. I want to intervene as little as possible. Decide the priorities yourself; don't stop to ask me to pick, and don't hand back a plan when you could have built it.

You are operating autonomously. The user is not watching in real time and cannot answer questions mid-task, so asking "Want me to…?" or "Shall I…?" will block the work. For reversible actions that follow from the original request, proceed without asking. Offering follow-ups after the task is done is fine; asking permission after already discussing with the user before doing the work is not. Before ending your turn, check your last paragraph. If it is a plan, an analysis, a question, a list of next steps, or a promise about work you have not done ("I'll…", "let me know when…"), do that work now with tool calls. End your turn only when the task is complete or you are blocked on input only the user can provide.

**Orient first, and treat the invariants as load-bearing.** Nothing catches a regression now except you, so ground your work in the real code before you touch it. Read the two docs above for what exists, what was fixed, and the known-limitations and not-yet-wired items they surfaced — your richest seam of upgrades (e.g. the near-real-time Boise `BPD_CallsForService` feed, the stubbed OpenMHz scanner that always returns nothing, the skipped ITD camera API). Read `CLAUDE.md`; skim the fetchers and both region packs. Preserve the invariants the recent fix branch established: fetchers throw on contract drift rather than returning `[]`; normalizers derive `updatedAt` from feed fields, never wall-clock now; a crime source's fetch window must match the aggregator's expiry for it; the region pack is the only extension seam, and the aggregator stays region-agnostic. Constraints: this runs on my machine, not a cloud service; keep new sources free and keyless (paid APIs stay off); don't spend money or take on a credential I haven't provided.

**Explore, decide, then build — don't stop between.** Sweep the opportunity space across these axes, weighted to what this is (a real-time situational-awareness map for DC and Boise): upgrades to existing sources (missed or richer endpoints, near-real-time vs historical, coverage, the stubbed and link-out-only sources); new free sources; new capabilities (there's a dead notifications service already in the tree; also history and trends, proximity and routing, saved views, mobile/PWA, sharing); new regions (the architecture is built for it); and platform (real persistence — the database is in-memory today — performance, installability). Rank what you find by impact against effort yourself, keep a dated catalog in `docs/` as the record of what you chose and why, and then build top-down: take on whole features end to end and chain them, working as far down the list as you can. Be bold in scope and disciplined in build — the largest actions means more features shipped, not more gold-plating on each; do the simplest thing that works and skip speculative abstractions.

**These gates are what let you act boldly without me:**
- Work on branches and keep CI green; never leave `master` broken.
- Verify every change by actually running it — hit the endpoints, do a live fetch, drive the app — not just types and tests. This repo rewards live verification.
- Before you ship a batch, run an adversarial fresh-context review of your own diff (the pattern that caught two real regressions in the last cycle) and fix what holds up.
- After each feature or source you complete, verify it with a fresh-context subagent against the invariants above and the change's own intent before moving on.
- Match the existing Leaflet + Tailwind UI (it has a persistent dark mode) for any new frontend rather than inventing an aesthetic.

**Ship, and batch the few things that need me.** Open a PR for each shippable branch and keep it green. Merge autonomously if the environment permits it; if a merge is blocked, don't fight the block — leave the branch green and ready and keep building other work. When you hit anything you genuinely can't do alone — a paid API or credential, a destructive or irreversible action, or a real judgment call that's mine — set it aside, note it, and continue on what you can. Only stop when you've run out of high-value work you can do autonomously.

Delegate independent subtasks to subagents and keep working while they run. Intervene if a subagent goes off track or is missing relevant context.

Before reporting progress, audit each claim against a tool result from this session. Only report work you can point to evidence for; if something is not yet verified, say so explicitly. Report outcomes faithfully: if tests fail, say so with the output; if a step was skipped, say that; when something is done and verified, state it plainly without hedging.

You have ample context remaining. Do not stop, summarize, or suggest a new session on account of context limits. Continue the work.

When you finish, write me one summary for a cold read: drop any working shorthand, use complete sentences, and spell out each file, branch, or PR you name. Lead with the outcome — what you shipped and what's green and ready for my review — then a single short list of the things that need a decision or a credential from me, each explained as if new.
